{
  "rules": {
    "v1": {
      "tables": {
        "$code": {
          ".read": "auth != null",
          ".write": "auth != null && newData.exists() && (!data.exists() || data.child('uids/w').val() === auth.uid || data.child('uids/b').val() === auth.uid || (!data.child('uids/w').exists() && newData.child('uids/w').val() === auth.uid) || (!data.child('uids/b').exists() && newData.child('uids/b').val() === auth.uid))",
          ".validate": "newData.hasChildren(['code', 'seq', 'state', 'uids']) && newData.child('code').val() === $code && ((!data.exists() && newData.child('seq').val() === 1) || (data.exists() && newData.child('seq').val() === data.child('seq').val() + 1)) && (!data.child('uids/w').exists() || newData.child('uids/w').val() === data.child('uids/w').val()) && (!data.child('uids/b').exists() || newData.child('uids/b').val() === data.child('uids/b').val())"
        }
      },
      "seen": {
        "$code": {
          ".read": "auth != null",
          "$seat": {
            ".write": "auth != null && root.child('v1/tables/' + $code + '/uids/' + $seat).val() === auth.uid",
            ".validate": "newData.isNumber() && newData.val() <= now + 5000"
          }
        }
      },
      "users": {
        ".read": "auth != null",
        ".indexOn": [
          "w"
        ],
        "$acc": {
          ".write": "auth != null && newData.exists()",
          ".validate": "newData.hasChildren(['id', 'rev']) && newData.child('id').val() === $acc && ((!data.exists() && newData.child('rev').val() === 1) || (data.exists() && newData.child('rev').val() === data.child('rev').val() + 1))"
        }
      },
      "photos": {
        "$acc": {
          ".read": "auth != null",
          ".write": "auth != null",
          ".validate": "newData.isString() && newData.val().length < 40000 && newData.val().beginsWith('data:image/jpeg;base64,')"
        }
      }
    }
  }
}
